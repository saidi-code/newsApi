name: VM Deployer

on:
  push:
    branches: [deploy-prod]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Deploy and restart server
        run: |
          cd /home/achraf/newsApi
          npm install
          npm PORT=3000 JWT_SECRET=*sdsd!sdfd12AFB$f ENV=production  run start:prod
